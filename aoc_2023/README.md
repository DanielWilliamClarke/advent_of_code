# AdventOfCode2023 🎄

## Install dependencies

```bash
    brew install make
    brew install cmake
    brew install llvm
```

## Build, test and run

```bash
# Debug
    make build_debug
    make runtests_debug
    make run_debug

# Release
    make build_release
    make run_release
```
