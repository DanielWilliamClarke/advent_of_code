# AdventOfCode2023 🎄

## Install dependencies

```bash
    brew install cmake
    brew install conan
    brew install llvm
```

## Build, test and run

```bash
    make install 
    make build
    make runtests
    make run
```
